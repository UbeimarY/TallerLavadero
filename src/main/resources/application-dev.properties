# Perfil: desarrollo local
# Activar con: --spring.profiles.active=dev
# o en IntelliJ → Edit Configurations → Active profiles: dev

spring.jpa.hibernate.ddl-auto=create
spring.jpa.show-sql=true

# Carga datos de prueba (DataInitializer solo se activa con este perfil)
app.init-data=true
